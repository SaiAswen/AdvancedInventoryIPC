# AdvancedInventoryIPC
Inventory Management System (IMS) in C++ using Windows Named Pipes for IPC. Enables real-time updates and efficient inventory management by allowing two separate programs (server and client) to communicate via a pipe, eliminating file-based communication. Includes add, search, and delete inventory functionality.
